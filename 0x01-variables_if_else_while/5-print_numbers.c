#include <stdio.h>
/**
 * main - Entry point
 * Description:print numbers from 0 to 10
 * Return: always 0 (success)
 */
int main(void)
{
	int num = 0;

	for (; num < 10; i++)
	printf("%d\n", num);
return (0);
}
