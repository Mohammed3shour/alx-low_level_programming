#include <stdio.h>
/**
 * main - Entry point
 * Description:print numbers from 0 to 10
 * Return: always 0 (success)
 */
int main(void)
{
	int num = 0;

	while (num <= 9)
	{
	printf("%i\n", num);
	num++;
	}
return (0);
}
