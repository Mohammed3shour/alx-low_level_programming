#include <unistd.h>
/**
 * print_rev - prints a reversed string, followed by a new line
 * @s: pointer to thestring to print
 * Return:void
*/
void print_rev(char *s)
{
	while (s[i])
	i++;

	while (i--)
	{
	_putchar(s[i]);
	}
	_putchar('\n');
}
